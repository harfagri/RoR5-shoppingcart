.col-md-10.center-block.no-float
	%article.card.large-padding
		%h1 El pago ha sido procesado
		%p Los accesos a la descarga han sido enviados al correo proporcionado
		%h2 Resumen de compra
		%table.table.table-striped.table-hover.medium#table_shopping_cart
			%thead
				%td Producto
				%td Precio
			%tbody
				-@shopping_cart.in_shopping_carts.each do |i_sh|
					-product = i_sh.product
					%tr{id: "#{product.id}"}
						%td= product.name
						%td= product.pricing
						